# Operations-controlled inference and current-event Theatre

Owner contract: canonical Memory `docs/architecture/PANDORA_INTELLIGENCE_ROUTER_BASELINE_V1.md`.
Source base: ea0387277d0a9f0eddec20e51c46e609279222e7. Continuation: OPS-FINISH-20260926-0049 / Operations Room #714.

This isolated lane does not overwrite PR741, the unpublished Router worktree, existing M3 adapters, Memory, or the historical Build Theatre projection. It extends reusable inference policy and current-event integration on canonical source. A policy module is not a live service, and source/test completion is not deployment, worker enrollment, or full acceptance.

The ten capability classes are imported from the existing approved-performance client, avoiding a divergent class registry. Security eligibility is mandatory. Explicit owner overrides are principal/task/expiry-bound. High-risk work ranks capability strength before price. Only current, approved, revision-and-configuration-compatible numeric Memory evidence may influence ranking; missing history remains unknown. Unknown cost ceilings are not admitted. Text context admission uses a conservative UTF-8 byte ceiling; supported image transports require an approved per-image token ceiling. No URL fetching, provider credentials, model approval, training, or worker creation is introduced.

`local_private` requests must be classified and executed on the phone before any sensitive material is sent to a cloud service. Cloud service rejection cannot undo prior transmission. The policy explicitly refuses local-private execution inside a cloud boundary; this is not a claim of physical-phone acceptance.

Source implementation is staged in this branch. Native durable lifecycle, authenticated serving, canonical verification, Memory delivery and live-event integration require their dedicated behavior/race tests and release readback before this lane can claim completion. No production approvals, catalogs, workers, channels or credentials are seeded by source publication.
