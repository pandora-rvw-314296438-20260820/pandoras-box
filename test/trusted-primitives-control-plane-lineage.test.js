'use strict';const test=require('node:test');const assert=require('node:assert/strict');
const {projectVersionPrimitiveLineage,primitiveUpgradeLineage}=require('../packages/primitives/src/control-plane-lineage');const H='sha256:'+'a'.repeat(64),B='sha256:'+'b'.repeat(64),C='sha256:'+'c'.repeat(64),D='sha256:'+'d'.repeat(64);
test('composition lineage preserves exact primitive identities',()=>{const out=projectVersionPrimitiveLineage({composition:{manifestDigest:H,primitives:[{name:'pandora-b',version:'1.0.0',trustState:'EXPERIMENTAL',definitionDigest:B,sourceDigest:C,configurationDigest:D,customizationDigest:H},{name:'pandora-a',version:'1.0.0',trustState:'TRUSTED',definitionDigest:C,sourceDigest:D,configurationDigest:H,customizationDigest:B}]},verificationEvidenceByPrimitive:{'pandora-a':'ev-a'}});assert.equal(out.primitiveCount,2);assert.deepEqual(out.primitives.map(x=>x.primitiveName),['pandora-a','pandora-b']);assert.equal(out.primitives[0].verificationEvidenceId,'ev-a');});
test('lineage rejects mutable/missing digests',()=>{assert.throws(()=>projectVersionPrimitiveLineage({composition:{manifestDigest:'latest',primitives:[{}]}}),/immutable/);});
test('upgrade lineage binds exact plan and evidence',()=>{const rows=primitiveUpgradeLineage({plan:{planDigest:H,steps:[{name:'pandora-auth',fromVersion:'1.0.0',toVersion:'1.1.0',decision:'AUTO'}]},outcomes:{'pandora-auth':{outcome:'VERIFIED',migrationSetDigest:B,verificationEvidenceId:'ev-1'}}});assert.equal(rows[0].outcome,'VERIFIED');assert.equal(rows[0].upgradePlanDigest,H);});
