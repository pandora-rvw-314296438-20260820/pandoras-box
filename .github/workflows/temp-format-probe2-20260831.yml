name: Temporary app-scoped Dart format probe

on:
  push:
    branches:
      - codex/format-probe2-20260831

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out probe source
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - name: Install pinned Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          flutter-version: '3.47.0'
          channel: stable
          cache: true
      - name: Apply app-scoped canonical formatter and retire probe
        run: |
          cd apps/pandora-mobile
          dart format \
            lib/core/data/project_experience_api.dart \
            lib/features/diagnostics/developer_diagnostics_screen.dart \
            lib/features/simple/project_experience_v2.dart
          cd ../..
          rm .github/workflows/temp-format-probe2-20260831.yml
          git config user.name 'pandora-format-bot'
          git config user.email 'actions@users.noreply.github.com'
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m 'style(mobile): apply app-scoped Dart formatter'
          git push origin HEAD:codex/format-probe2-20260831
