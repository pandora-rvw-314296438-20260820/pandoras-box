name: Temporary Dart format probe

on:
  push:
    branches:
      - codex/format-probe-20260831

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-24.04
    steps:
      - name: Check out probe source
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - name: Install pinned Flutter
        uses: subosito/flutter-action@1a449444c387b1966244ae4d4f8c696479add0b2
        with:
          flutter-version: '3.47.0'
          channel: stable
          cache: true
      - name: Apply canonical formatter and retire probe
        run: |
          dart format \
            apps/pandora-mobile/lib/core/data/project_experience_api.dart \
            apps/pandora-mobile/lib/features/diagnostics/developer_diagnostics_screen.dart \
            apps/pandora-mobile/lib/features/simple/project_experience_v2.dart
          rm .github/workflows/temp-format-probe-20260831.yml
          git config user.name 'pandora-format-bot'
          git config user.email 'actions@users.noreply.github.com'
          git add -A
          git diff --cached --quiet && exit 0
          git commit -m 'style(mobile): apply canonical Dart formatter'
          git push origin HEAD:codex/format-probe-20260831
